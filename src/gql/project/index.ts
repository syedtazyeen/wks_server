import { queries } from "./queries.project";
import { mutations } from "./mutations.project";
import { typedefs } from "./typedefs.project";
import { resolvers } from "./resolvers.project";


export const Project = { queries, mutations, typedefs, resolvers }